# Pirdfy - Bird Feeder Camera Detector
# Python Dependencies
#
# NOTE: The following are installed via apt (not pip):
#   - picamera2 (python3-picamera2)
#   - numpy (python3-numpy)
#   - PIL/Pillow (python3-pil)
#   - opencv (python3-opencv)
#
# The venv is created with --system-site-packages to use these.

# Web Framework
flask>=3.0.0
flask-socketio>=5.3.0
flask-cors>=4.0.0

# Bird Detection (YOLOv8)
ultralytics>=8.0.0

# Database
SQLAlchemy>=2.0.0

# Configuration
PyYAML>=6.0

# System Monitoring
psutil>=5.9.0

# WebSocket support
python-socketio>=5.10.0
eventlet>=0.34.0

# Utilities
python-dateutil>=2.8.0
requests>=2.31.0
